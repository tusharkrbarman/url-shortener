[project]
name = "url-shortener"
version = "0.1.0"
description = "Production-shaped URL shortening service using Python standard library components."
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]

